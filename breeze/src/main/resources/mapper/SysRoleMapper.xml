<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.breeze.boot.system.mapper.SysRoleMapper">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.breeze.boot.system.domain.SysRole" id="sysRoleMap">
        <id property="id" column="id"/>
        <result property="roleCode" column="role_code"/>
        <result property="roleName" column="role_name"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="createName" column="create_name"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="isDelete" column="is_delete"/>
        <result property="deleteBy" column="delete_by"/>
    </resultMap>

    <select id="listRoleByUserId" resultType="com.breeze.boot.system.domain.SysRole">
        SELECT role.id,
               role.role_code,
               role.role_name
        FROM sys_role AS role
                 LEFT JOIN sys_user_role AS ru ON ru.role_id = role.id
        WHERE role.is_delete = 0
          AND ru.is_delete = 0
          AND ru.user_id = #{userId}
        ORDER BY role.create_time DESC
    </select>

</mapper>

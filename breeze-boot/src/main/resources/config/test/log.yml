# 日志拦截开关
breeze:
  log:
    enable: true

logging:
  file:
    name: ./logs/${spring.application.name}/${spring.application.name}.log
  config: classpath:logback-spring.xml
  level:
    org:
      flowable: debug